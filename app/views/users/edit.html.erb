<%if session[:user_id] %>

<h1>Editing user</h1>

<%= render 'form' %>

<%= link_to 'Show', @user %> |
<%= link_to 'Back', users_path %>

<% else %>
U hebt niet de rechten om deze pagina te bekijken

<% end %>